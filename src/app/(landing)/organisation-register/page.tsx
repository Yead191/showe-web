import OrganisationRegister from "@/features/auth/components/OrganisationRegister";

export const metadata = {
    title: "Organization Registration | Showe",
    description: "Register your organization and start creating interactive event programs with Showe.",
};

export default function OrganisationRegisterPage() {
    return (
        <OrganisationRegister />
    );
}
